import { ActionButtonColumn } from './action-button-column';

describe('ActionButtonColumn', () => {
  it('should create an instance', () => {
    expect(new ActionButtonColumn()).toBeTruthy();
  });
});
